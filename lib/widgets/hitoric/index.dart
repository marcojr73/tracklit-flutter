
import 'package:flutter/material.dart';

class Historic extends StatelessWidget {
  const Historic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("I'm the historic widget"),
    );
  }
}